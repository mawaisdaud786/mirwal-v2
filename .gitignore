# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated at build time by scripts/generate-sitemap.mjs
apps/storefront/public/sitemap.xml

# Server secrets and local database config
server/.env
server/.env.local

# Local development MariaDB (lives outside the repo, but guard anyway)
.devdb/

# Local frontend env
.env
.env.local
apps/*/.env
apps/*/.env.local
!apps/*/.env.example

# Seller verification documents (identity papers — never committed)
server/storage/

# Stray build archive and refactor scratch artifacts (see DEPLOYMENT notes)
server.zip
*.zip
.migration/
